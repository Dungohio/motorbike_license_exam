$kind: http-request
description: |-
  Xóa chủ đề (chỉ admin).

  **Trả về:** thông báo đã xóa.
url: "{{baseUrl}}/categories/:id"
method: DELETE
headers:
  Authorization: Bearer {{token}}
pathVariables:
  id: ""
examples: ./.resources/Delete category 👑.resources/examples
order: 4000
