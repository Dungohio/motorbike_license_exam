$kind: http-request
url: "{{baseUrl}}/questions/upload-image"
method: POST
headers:
  Authorization: Bearer {{token}}
body:
  type: formdata
  content:
    - type: file
      key: image
      src:
        - ""
order: 3000
